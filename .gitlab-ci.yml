image: docker:latest 

variables: 
  DOCKER_DRIVER: overlay2 
  DOCKER_TLS_CERTDIR: ""
  
default:
  before_script:
    - docker info

stages:     
  - build

build-job:
  stage: build
  script:
    - docker build -t 2048-game_image .
    - docker login -u nick62rus --password $DOCKER_HUB_TOKEN
